An API wrapper for all contentmine code bases.

Defines routes and calls to access the code bases and returns JSON output.

Results of calls may be placed in files on disk, accessible via other means.

